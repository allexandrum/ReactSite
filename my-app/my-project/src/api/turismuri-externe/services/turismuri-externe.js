'use strict';

/**
 * turismuri-externe service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::turismuri-externe.turismuri-externe');
